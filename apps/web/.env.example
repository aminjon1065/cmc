# Public — exposed to the browser. Do not put secrets here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Private — server-only.
# (Add Auth.js secrets, OAuth provider IDs, etc. here when auth is wired.)
